machine:
  environment:
    PATH: "${PATH}:${HOME}/${CIRCLE_PROJECT_REPONAME}/node_modules/.bin"

dependencies:
  cache_directories:
    - ~/.cache/yarn
  override:
    - yarn

deployment:
  production:
    branch: master
    commands:
      - npm run installDeps
      - npm run deploy

test:
  override:
    - echo "No tests as yet"
